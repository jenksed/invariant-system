# Composed Workflows

These playbooks compose existing Project Arsenal capabilities into repeatable operating sequences.

A workflow does not duplicate the underlying prompts. It defines ordering, handoff expectations, and stopping conditions between independently reusable assets.

The machine-readable asset relationships live in `arsenal/registry.json`.
