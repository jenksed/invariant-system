name: ARS-06 Closeout Materialize

on:
  push:
    branches:
      - agent/ars-06-dagger-executable-world
    paths:
      - ".github/workflows/ars-06-materialize.yml"

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/ars-06-dagger-executable-world

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Regenerate catalog and public status
        run: |
          python3 scripts/arsenal_audit.py --write-catalog
          python3 scripts/materialize-ars06-closeout.py

      - name: Validate integrated state before write
        run: |
          python3 scripts/arsenal_dagger.py validate
          python3 scripts/test-arsenal-dagger.py
          python3 scripts/arsenal_substrate.py validate
          python3 scripts/test-arsenal-substrate.py
          python3 scripts/arsenal_graph.py validate
          python3 scripts/test-arsenal-graph.py
          python3 scripts/arsenal_compile.py verify
          python3 scripts/capability_audit.py
          python3 scripts/arsenal_audit.py

      - name: Commit generated and status files only
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "Project Arsenal CI"
          git config user.email "jenksed@noreply.local"
          git add CATALOG.md README.md docs/roadmap/capability-system.md
          git diff --cached --quiet && exit 0
          changed="$(git diff --cached --name-only)"
          allowed=$'CATALOG.md\nREADME.md\ndocs/roadmap/capability-system.md'
          test "$changed" = "$allowed"
          git commit -m "Record ARS-06 delivery and advance frontier"
          git push origin HEAD:agent/ars-06-dagger-executable-world
