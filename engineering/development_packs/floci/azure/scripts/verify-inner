#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/common.sh"
guard_endpoint
wait_ready
python3 -c 'import azure.storage.blob, azure.storage.queue'
printf 'FLC-04 Azure inner gate: PASS\n'
