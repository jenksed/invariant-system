services:
  floci-gcp:
    image: ${FLOCI_GCP_IMAGE:-floci/floci-gcp:0.6.0}
    ports:
      - "127.0.0.1:4588:4588"
    environment:
      FLOCI_GCP_STORAGE_MODE: memory
      FLOCI_GCP_DEFAULT_PROJECT_ID: ${FLOCI_GCP_PROJECT:-floci-local}
      FLOCI_GCP_BASE_URL: http://localhost:4588
