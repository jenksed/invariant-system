# AGENTS.md

This is a governed fixture repository for the Project Arsenal Repository
Recon Method Evaluation (ARS-04).

It contains:
- engineering/doctrine/ (canonical contracts);
- arsenal/capabilities/ (canonical capability fragments);
- scripts/ (validator surfaces);
- evaluation/method-records/ (existing QMR evidence).

The recon method is expected to find all of these canonical surfaces and
bind its architecture anchor to the canonical ownership layers.
