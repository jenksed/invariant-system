@AGENTS.md

# Claude Code Specific Instructions

`AGENTS.md` is the primary repository contract. Treat the Engineering Doctrine it references as paramount for planning, implementation, review, verification, and completion.

Do not duplicate the full doctrine here. Keep Claude-specific guidance limited to behavior that is genuinely specific to Claude Code.

Prefer repository-native deterministic checks over additional model reasoning whenever those checks can answer the question reliably. Do not claim completion without the evidence required by `AGENTS.md`.
